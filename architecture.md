# Architecture Overview

RefSynは、Kanonの操作ログから主問題と部分問題を抽出し、既存のPBE合成器を用いて最終プログラムを構築する。現在の実装では環境中の値を`Int`や`List[Int]`として扱い、操作列はグラフとして解析される。

```mermaid
flowchart LR
    subgraph Kanon[入力: テストケース]
        E[環境\n(Int, List[Int])]
        OP[操作列\n(操作グラフ)]
    end

    E -->|解析| G[操作グラフと環境の統合]
    OP --> G
    G -->|コンパイル| P[操作プログラム × N]
    P -->|差分解析| D[差分PBE仕様 × N]
    E -->|テストケース分解| S[部分問題のPBE仕様 × N]

    S --> Synth[既存PBE合成器]
    D --> Synth

    Synth --> R[部分問題の解]
    R -->|雛形と統合| M[主問題の解の雛形]
    M --> Final[最終プログラム]
```

## フロー詳細
1. **入力取得**: Kanonから環境(`Int`や`List[Int]`)と操作列を受け取る。
2. **操作解析**: 操作列を操作グラフとして表現し、環境と統合する。
3. **コンパイル**: 操作グラフから各テストケース用の操作プログラムを生成。
4. **差分解析**: 主問題との差分から部分問題のPBE仕様を抽出。
5. **部分問題合成**: 既存PBE合成器により部分問題を解き、解の雛形と統合。
6. **最終出力**: 得られた部分問題の解を統合し、主問題の解を構成する。

このパイプラインにより、ユーザ操作から高水準なプログラムを合成できる。
