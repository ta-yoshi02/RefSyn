# アルゴリズム提案：グラフ同型性判定を用いた操作の共通部分検出

## 1. 目的

`unify_operation_graphs`関数が採用する探索的アプローチとは別に、グラフ理論における「グラフ同型性」の考え方を応用し、より形式的で構造的なアプローチで2つの操作シーケンスの共通部分と差分を検出するアルゴリズムを提案する。

このアプローチは、操作のシーケンスそのものを一つのグラフ（以下、**操作グラフ**）としてモデル化し、グラフ同士を比較することで、本質的な構造の類似性を捉えることを目指す。

## 2. 中心的なアイデア：操作グラフ (Operation Graph)

このアルゴリズムの核心は、操作履歴を単なるリストとしてではなく、操作間の依存関係を辺（エッジ）で表現した有向グラフとして扱うことにある。

### 操作グラフのノード

操作グラフの「ノード」は、`Op`構造体、すなわち**一つ一つの操作イベントそのもの**である。これは`AddNode`や`ExistNode`といったノード操作だけでなく、**`AddEdge`のようなエッジ操作も等しくノードとして扱われる**ことが極めて重要である。

-   `Op { id: "op1", kind: GraphOp::Node(NodeExpr::AddNode(...)) }` は、操作グラフにおける**タイプ`AddNode`のノード**となる。
-   `Op { id: "op2", kind: GraphOp::Edge(EdgeExpr::AddEdge(...)) }` は、操作グラフにおける**タイプ`AddEdge`のノード**となる。

各ノードは、元の操作の種類（`AddNode`, `AddEdge`など）と、その属性（`label`や`id`など）を保持する。

### 操作グラフのエッジ（操作間の役割的参照関係）

操作グラフの「エッジ」は、ある操作が他の操作を特定の役割（`from`や`to`など）で引数として利用する関係性を表す、**ラベル付き**の有向エッジである。

-   **定義**: 操作Bが、その定義の中で操作Aの`id`を特定の役割（例：`from`フィールド）で参照している場合、その役割を示すラベルを持つ参照エッジが存在すると定義する。
-   **エッジの向き**: 参照する側から、参照される側へエッジを引く (`操作B --役割ラベル--> 操作A`)。

**具体例:**

`Op { id: "b1", ... }` (オブジェクト`T1`に対応), `Op { id: "b0", ... }` (値`V1`に対応), `Op { id: "b2", kind: AddEdge { from: "b1", to: "b0", label: "next" } }` という3つの操作は、擬似コードにおける `T1.next = V1` という代入操作に相当する。

この関係性を操作グラフで厳密に表現すると、`AddEdge`操作であるノード`b2`が、他のノードをどのような役割で参照しているかを示す、ラベル付きの有向エッジで表現できる。

-   `ノード b2 --from--> ノード b1`
-   `ノード b2 --to--> ノード b0`

このように、操作グラフは、各操作をノードとし、操作が他の操作を引数として利用する際の役割（`from`, `to`など）を辺のラベルで表現した、有向ラベル付きグラフとなる。

## 3. アルゴリズムのステップ

### ステップ1：操作グラフの構築

まず、入力となる2つの操作シーケンス `a: Vec<Op>` と `b: Vec<Op>` から、それぞれ操作グラフ `G_a` と `G_b` を構築する。

### ステップ2：最大共通部分グラフ（MCS）の探索

構築した2つの操作グラフ `G_a` と `G_b` の間で、**最大共通部分グラフ（Maximum Common Subgraph, MCS）**を見つけ出す。これは、`find_mcs`関数によって実現される。

-   **目的**: このステップの目的は、2つのグラフ間で構造的に最もよく似ている部分グラフの対応付け（マッピング）を確立することである。
-   **バックトラッキング**: `find_mcs`は、`backtrack_mcs`という再帰的なバックトラッキング関数を用いて、最適なマッピングを探索する。
-   **一貫性チェック**: `backtrack_mcs`の内部では、新しいノードペア（`g1_node`, `g2_node`）をマッピングに追加する際に、`is_consistent`関数を呼び出す。この関数は、新しいマッピングが既存のマッピングと構造的に矛盾しないこと（つまり、エッジの接続関係が保たれていること）を検証する。
-   **同型性の判定**: MCS探索後、見つかった共通部分グラフのノード数が元のグラフのノード数と一致しない場合、2つのグラフは同型でないと判断し、処理を中断する。

### ステップ3：属性に基づく最終分類

MCSによって得られたマッピングを元に、各操作を「共通」か「差分」かに最終的に分類する。

1.  **マッピングの検証**: MCSマッピングに含まれる操作ノードのペア（`op_a`, `op_b`）を一つずつ検証する。
2.  **属性比較**: `attributes_match`関数を用いて、ペアの属性を比較する。この比較ロジックが、差分を決定する上で重要となる。
    -   **構造が一致、かつ属性も一致 → 共通**
    -   **構造が一致、しかし属性が不一致 → 差分**

このプロセスにより、「構造的には同じ役割を果たしているが、具体的な値や参照先が異なる」操作を正確に「差分」として分類することが可能になる。

## 4. 実装詳細

このアルゴリズムは、`src/isomorphism.rs`内の`unify_isomorphic_graphs`関数として実装されている。

### ラベルと属性のマッチング

分類の精度は、`node_labels_match`と`attributes_match`という2つの関数に依存している。

#### `node_labels_match`

この関数は、2つの操作ノードが**同じ種類**であるかを大まかに判定する。`std::mem::discriminant`を用いて、`enum`のどのバリアントであるか（例：`GraphOp::Node`か`GraphOp::Edge`か）を比較する。

-   `Node`と`Node` → `true`
-   `Edge`と`Edge` → `true`
-   `Variable`と`Variable` → `true`
-   その他 → `false`

#### `attributes_match`

この関数は、`node_labels_match`で同じ種類だと判断されたノードペアが、意味的にも同一であるかを詳細に判定する。

-   **`AddNode`**:
    -   両方の `is_literal` が `false` の場合、`id` や `label` が異なっていても、構造的に対応する新しいノードと見なされ、**共通** (`true`) となる。
    -   それ以外の場合、`label` が一致する場合にのみ**共通** (`true`) となる。
-   **`ExistNode`**: `id` が一致する場合に**共通** (`true`) となる。
-   **`NullNode`**: 常に**共通** (`true`) となる。
-   **`AddEdge` / `EditEdgeReference`**: `label` が一致する場合に**共通** (`true`) となる。
-   **`AddVariable`**: `label` が一致する場合に**共通** (`true`) となる。

この詳細な属性比較により、構造的に類似していても属性が異なる操作は「差分」として正しく分類される。

## 5. 結論

このアルゴリズムは、操作シーケンスの比較という問題を、より形式的なグラフ理論の問題として捉え直すものである。操作グラフという中間表現とMCS探索を組み合わせることで、操作間の依存関係を明確にし、構造的な類似性の発見と、属性レベルでの詳細な差分検出を、系統立てて行うことができるようになる。