# アルゴリズム提案：グラフ同型性判定を用いた操作の共通部分検出

## 1. 目的

`unify_operation_graphs`関数が採用する探索的アプローチとは別に、グラフ理論における「グラフ同型性」の考え方を応用し、より形式的で構造的なアプローチで2つの操作シーケンスの共通部分と差分を検出するアルゴリズムを提案する。

このアプローチは、操作のシーケンスそのものを一つのグラフ（以下、**操作グラフ**）としてモデル化し、グラフ同士を比較することで、本質的な構造の類似性を捉えることを目指す。

## 2. 中心的なアイデア：操作グラフ (Operation Graph)

このアルゴリズムの核心は、操作履歴を単なるリストとしてではなく、操作間の依存関係を辺（エッジ）で表現した有向グラフとして扱うことにある。

### 操作グラフのノード

操作グラフの「ノード」は、`Op`構造体、すなわち**一つ一つの操作イベントそのもの**である。これは`AddNode`や`ExistNode`といったノード操作だけでなく、**`AddEdge`のようなエッジ操作も等しくノードとして扱われる**ことが極めて重要である。

-   `Op { id: "op1", kind: GraphOp::Node(NodeExpr::AddNode(...)) }` は、操作グラフにおける**タイプ`AddNode`のノード**となる。
-   `Op { id: "op2", kind: GraphOp::Edge(EdgeExpr::AddEdge(...)) }` は、操作グラフにおける**タイプ`AddEdge`のノード**となる。

各ノードは、元の操作の種類（`AddNode`, `AddEdge`など）と、その属性（`label`や`id`など）を保持する。

### 操作グラフのエッジ（操作間の役割的参照関係）

操作グラフの「エッジ」は、ある操作が他の操作を特定の役割（`from`や`to`など）で引数として利用する関係性を表す、**ラベル付き**の有向エッジである。

-   **定義**: 操作Bが、その定義の中で操作Aの`id`を特定の役割（例：`from`フィールド）で参照している場合、その役割を示すラベルを持つ参照エッジが存在すると定義する。
-   **エッジの向き**: 参照する側から、参照される側へエッジを引く (`操作B --役割ラベル--> 操作A`)。

**具体例:**

`Op { id: "b1", ... }` (オブジェクト`T1`に対応), `Op { id: "b0", ... }` (値`V1`に対応), `Op { id: "b2", kind: AddEdge { from: "b1", to: "b0", label: "next" } }` という3つの操作は、擬似コードにおける `T1.next = V1` という代入操作に相当する。

この関係性を操作グラフで厳密に表現すると、`AddEdge`操作であるノード`b2`が、他のノードをどのような役割で参照しているかを示す、ラベル付きの有向エッジで表現できる。

-   `ノード b2 --from--> ノード b1`
-   `ノード b2 --to--> ノード b0`

このように、操作グラフは、各操作をノードとし、操作が他の操作を引数として利用する際の役割（`from`, `to`など）を辺のラベルで表現した、有向ラベル付きグラフとなる。

## 3. アルゴリズムのステップ

### ステップ1：操作グラフの構築

まず、入力となる2つの操作シーケンス `a: Vec<Op>` と `b: Vec<Op>` から、それぞれ操作グラフ `G_a` と `G_b` を構築する。

### ステップ2：グラフ同型性判定とマッピング

構築した2つの操作グラフ `G_a` と `G_b` が同型であるかを判定する。現在の仮定では、2つのグラフはほとんどの場合で同型であるため、同型でない場合はエラーとして処理を中断する。

-   **目的**: このステップの目的は、**構造的に**同型なグラフのマッピングを確立することである。ノードが持つ属性の完全な一致は、この段階では問わない。
-   **同型性判定**: `find_mcs`関数を用いて、最大共通部分グラフを探す。操作の数が同じであるため、見つかった共通部分グラフのノード数が元のグラフのノード数と一致しない場合、2つのグラフは同型でないと判断し、処理を中断する。

### ステップ3：属性に基づく最終分類

各操作を「共通」か「差分」かに最終的に分類する。

1.  **マッピングの検証**: 同型マッピングに含まれる操作ノードのペア（`op_a`, `op_b`）を一つずつ検証する。
2.  **属性比較**: `attributes_match`関数を用いて、ペアの属性を比較する。この比較ロジックが、差分を決定する上で重要となる。
    -   **構造が一致、かつ属性も一致 → 共通**
    -   **構造が一致、しかし属性が不一致 → 差分**

このプロセスにより、「同型性判定の時には問題なかったが実際には異なるもの」を正確に「異なるもの」として分類することが可能になる。

## 4. 実装

このアルゴリズムは、`src/isomorphism.rs`内の`unify_isomorphic_graphs`関数として実装されている。

### 属性比較 (`attributes_match`)

`attributes_match`関数は、マッピングされたノード（操作）が意味的に同一であるかを判定する。この関数は、操作の種類に応じて以下の属性を比較する。

-   **`AddNode`**:
    -   両方のノードの `is_literal` が `false` の場合、`id` や `label` が異なっていても、それらは構造的に対応する新しいノードと見なされ、**共通** (`true`) と判断される。
    -   それ以外の場合（少なくとも一方がリテラルノードの場合）、`label` が一致する場合にのみ**共通** (`true`) と判断される。
-   **`ExistNode`**: `id` が一致する場合に**共通** (`true`) となる。
-   **`AddEdge`**: `label` が一致する場合に**共通** (`true`) となる。

この詳細な属性比較により、構造的に類似していても属性が異なる操作は「差分」として正しく分類される。

## 5. 結論

このアルゴリズムは、操作シーケンスの比較という問題を、より形式的なグラフ理論の問題として捉え直すものである。操作グラフという中間表現を導入することで、操作間の依存関係を明確にし、構造的な類似性の発見と、属性レベルでの詳細な差分検出を、系統立てて行うことができるようになる。
